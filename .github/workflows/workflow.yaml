name: First workflow
on: [workflow_dispatch]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some text
        run: echo "some text"
      - name: cfsf
        run: |
          node -v 
          npm -v 