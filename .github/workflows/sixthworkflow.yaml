name: six workflow
on:
  push:
    branches:
      - main   
    paths:
      - "**/info"
  issues:
    types: [edited, deleted]
  pull_request:
    types: [assigned, closed]
jobs:
    main:
      runs-on: ubuntu-latest
      steps:
        - name: test
          run: echo testing
        
