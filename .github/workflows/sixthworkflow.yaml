name: six workflow
on:
  push:
    branches:
      - main   
    paths:
      - "**.info"
jobs:
    main:
      runs-on: ubuntu-latest
      steps:
        - name: test
          run: echo testing
        
