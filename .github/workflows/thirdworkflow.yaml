name: 3workflow
on: [workflow_dispatch]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: command 
        run:  ls -la
      - uses: actions/checkout@v4  
      - name: list files
        run: ls -la