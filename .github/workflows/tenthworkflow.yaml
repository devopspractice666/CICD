name: tenth
on: [push]
jobs:
  mujob:
    runs-on: ubuntu-latest
    steps:
      - name: first
        id: context-expression
        run: |
          echo ${{ toJson(github) }}
          echo ${{ toJson(runner) }}
          echo ${{ toJson(job) }}
          echo ${{ toJson(steps) }}
