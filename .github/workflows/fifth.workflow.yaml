name: 5workflow
on: 
    issues:
      types: [edited, deleted]
    pull_request:
      types: [assigned, closed]

jobs:
    job:
      runs-on: ubuntu-latest
      steps:
        - name: event vars
          run: |
            echo $GITHUB_SHA
            echo $GITHUB_REF