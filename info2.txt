запуск вф по расписанию:
on.schedule:
  - cron: "* * * * *" // расписание в крон формате
минута час день месяца месяц день недели
есть онлайн генератор крон формата
- cron: "*/2 * * * *" // каждые две минуты как пример
гитхаб во время нагрузки может отложить наш шедул

on.workflow_dispatch // запуск при помощи кнопки
inputs:
        titleCustom:
          description: type the title
          type: string
          required: true
          default: "Hello world"
        selectCustom:
          description: select value
          type: choice
          required: true
          options:
            - value1
            - value2
            - value3
          default: value2
        checkCustom:
          description: checkbox
          type: boolean
          required: false

          добавление интерактивного поля для диспатч
          есть с вводом строки, выбором параметра и с булевым полем

${{ inputs.titleCustom }} получение поля в самом теле воркфлоу


workflow_dispatch через restapi
генерация токена в гитхабе для репозитория с actions read and write
токен в заголовке поле accept поле версии
пост запрос (все в документации)
refs:(ветка) inputs:(инпуты)

repository_dispatch
для удаленного запуска без кнопки
on.repository_dispatch.types
{{ github.event.client_payload.titleTest }}
все так же в документации
в конспекте должно быть нужное тело запроса с event type, client_payload и тд чтобы вывело наш titleTest


контекст, выражение
выражение - то что в скобках
контекст, откуда берем поля по типу github.[поля]

toJson(github) - передача объекта, в функцию, которая выводит содержимое контекста


run-name - поле для указания имени заголовка коммита на гитхаб экшнс

if : {{<выражение сравнения>}}